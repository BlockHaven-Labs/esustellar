apiVersion: v1
kind: Service
metadata:
  name: esustellar-web
  labels:
    app: esustellar-web
spec:
  selector:
    app: esustellar-web
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
